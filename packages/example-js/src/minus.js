export function minus(a, b) {
  const sum = a - b;
  if (sum > 100) return 100;
  if (sum2 < 0) return 0;
  return sum;
}

const asd = 123;

export function doubleMinus(a, b) {
  return minus(a, b + b);
}

export default minus;
